# @package _global_

# python main.py eval +experiment=birdnet_embeddings
defaults:
  - override /data: sounding_out_chorus
  - override /model: birdnet_embeddings
  - override /evaluator: birdnet_embedder
  - override /logger: null
