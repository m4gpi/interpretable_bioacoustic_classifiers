# @package _global_

# python main.py eval +experiment=birdnet
defaults:
  - override /data: sounding_out_chorus
  - override /model: birdnet
  - override /evaluator: birdnet_predictor
  - override /logger: null

project: birdnet
